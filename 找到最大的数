描述:乐乐获得4个最大数，请帮他编程找到最大的数。
输入描述：一行，4个整数，用空格分开。
输出描述：一行，一个整数，为输入的4个整数中最大的整数。
输入：5 8 2 5
输出：8
int main()
{
	int a[4] = { 0 };
	for (int i = 0; i < 4; i++)
	{
		scanf_s("%d", &a[i]);
	}
	int max = a[0];
	for (int i = 1; i < 4; i++)
	{
		if (a[i] > max)
		{
			max = a[i];
		}
	}
	printf("%d\n", max);
	return 0;
}
