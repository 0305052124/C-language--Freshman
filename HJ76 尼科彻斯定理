#include<stdio.h>
int main()
{
    int m;
    scanf("%d",&m); 
    int sum=m*(m-1)/2;
    int i;
    for(i=sum*2+1;i<(sum*2+2*m-1);i+=2)
    {
        printf("%d+",i);
    }
    if(i==(sum*2+2*m-1))
    {
        printf("%d",i);
    }
    return 0;
}
